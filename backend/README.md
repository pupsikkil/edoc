# ЭДО Система - Backend API

Backend на FastAPI для системы электронного документооборота.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
```

### Запуск базы данных (PostgreSQL)

Используйте Docker Compose:

```bash
docker-compose up -d postgres redis
```

Или установите PostgreSQL локально.

### Запуск приложения

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

API будет доступен по адресу: http://localhost:8000

Документация Swagger: http://localhost:8000/docs

## 📚 API Endpoints

### Авторизация
- `POST /api/auth/login` - Вход
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/refresh` - Обновление токена
- `GET /api/auth/me` - Текущий пользователь

### Компании
- `GET /api/companies/me` - Моя компания
- `PUT /api/companies/me` - Обновить компанию

### Документы
- `GET /api/documents/` - Список документов
- `GET /api/documents/{id}` - Документ по ID
- `POST /api/documents/` - Создать документ
- `PUT /api/documents/{id}` - Обновить документ
- `DELETE /api/documents/{id}` - Удалить документ

### Контрагенты
- `GET /api/partners/` - Список контрагентов
- `GET /api/partners/{id}` - Контрагент по ID
- `POST /api/partners/` - Создать контрагента
- `DELETE /api/partners/{id}` - Удалить контрагента

### Файлы
- `POST /api/files/upload` - Загрузить файл
- `GET /api/files/download/{path}` - Скачать файл

## 🗄️ База данных

Модели определены в `app/models.py`. Для создания таблиц:

```python
from app.database import engine, Base
Base.metadata.create_all(bind=engine)
```

Или используйте Alembic для миграций:

```bash
alembic init alembic
alembic revision --autogenerate -m "Initial migration"
alembic upgrade head
```

## 🔐 Безопасность

- JWT токены (access + refresh)
- Хэширование паролей (bcrypt)
- CORS настройки
- Валидация данных (Pydantic)

## 📝 TODO Stage 1 MVP

- [x] Базовая структура FastAPI
- [x] Модели БД (SQLAlchemy)
- [x] Авторизация (JWT)
- [x] API для компаний, документов, контрагентов
- [ ] Загрузка файлов (MinIO/S3)
- [ ] ЭЦП (подписание документов)
- [ ] Тесты (pytest)
- [ ] Docker Compose для разработки



