# 🚀 Быстрый старт - Подключение Frontend к Backend API

## ✅ Что было сделано

Frontend успешно подключён к Backend API. Реализовано:

- ✅ API клиент на базе axios (`lib/api.ts`)
- ✅ Авторизация и регистрация (`app/login`, `app/register`)
- ✅ Подключение страницы документов к реальному API
- ✅ Автоматическое обновление токенов
- ✅ Обработка ошибок API
- ✅ Перехватчики (interceptors) для всех запросов

## 📋 Шаги для запуска

### 1. Запуск Backend и сервисов

```bash
# Запустить Docker Compose (PostgreSQL, Redis, MinIO)
docker-compose up -d

# Запустить Backend API (в отдельном терминале)
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Backend будет доступен на: `http://localhost:8000`

### 2. Настройка Frontend

Создайте файл `.env.local` в корне проекта:

```bash
NEXT_PUBLIC_API_URL=http://localhost:8000
```

### 3. Запуск Frontend

```bash
npm run dev
```

Frontend будет доступен на: `http://localhost:3000`

## 🔍 Проверка работы

### 1. Тест API через Swagger UI

Откройте в браузере: `http://localhost:8000/docs`

Здесь можно:
- Проверить все доступные эндпоинты
- Протестировать API вручную
- Посмотреть схемы данных

### 2. Тест авторизации

1. Откройте `http://localhost:3000/login`
2. Зарегистрируйте нового пользователя на `/register`
3. Войдите в систему

### 3. Тест документов

1. После входа вы будете на `/documents`
2. Создайте документ через Swagger UI (если необходимо)
3. Страница автоматически загрузит документы из API

## 📁 Структура API интеграции

```
lib/
├── api.ts              # Основной API клиент
└── auth.ts             # Утилиты для авторизации

app/
├── login/page.tsx      # Страница входа (подключена к API)
├── register/page.tsx   # Страница регистрации (подключена к API)
└── documents/page.tsx  # Страница документов (подключена к API)
```

## 🔧 API клиент

### Основные методы

```typescript
import { authAPI, documentsAPI, partnersAPI } from '@/lib/api'

// Авторизация
authAPI.login({ email, password })
authAPI.register({ email, password, full_name, company_name, inn })
authAPI.getCurrentUser()

// Документы
documentsAPI.getAll({ skip, limit, status })
documentsAPI.getById(id)
documentsAPI.create(data)
documentsAPI.update(id, data)
documentsAPI.delete(id)

// Контрагенты
partnersAPI.getAll()
partnersAPI.create(data)

// И т.д. - смотри API_INTEGRATION.md
```

### Утилиты авторизации

```typescript
import { login, register, logout } from '@/lib/auth'

// Вход и сохранение токенов
await login(email, password)

// Регистрация и сохранение токенов
await register({ email, password, ... })

// Выход и очистка токенов
logout()
```

## 🛡️ Безопасность

### Автоматическое обновление токенов

API клиент автоматически обновляет токены при истечении:

1. При ошибке 401, клиент пытается обновить токен
2. Если успешно, запрос повторяется автоматически
3. Если не удалось, происходит logout

### Хранение токенов

Токены хранятся в `localStorage`:
- `access_token` - токен доступа (15 минут)
- `refresh_token` - токен обновления (7 дней)

### CORS

Backend настроен для работы с `http://localhost:3000`:
```python
allowed_origins = ["http://localhost:3000"]
```

## 📊 Примеры использования

### Полный пример компонента

```typescript
"use client"

import { useState, useEffect } from 'react'
import { documentsAPI } from '@/lib/api'

export default function MyComponent() {
  const [documents, setDocuments] = useState([])
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState('')

  useEffect(() => {
    loadDocuments()
  }, [])

  const loadDocuments = async () => {
    try {
      setLoading(true)
      const data = await documentsAPI.getAll({ skip: 0, limit: 20 })
      setDocuments(data)
      setError('')
    } catch (err: any) {
      setError(err.response?.data?.detail || 'Ошибка загрузки')
    } finally {
      setLoading(false)
    }
  }

  return (
    <div>
      {loading && <p>Загрузка...</p>}
      {error && <p className="text-red-500">{error}</p>}
      {documents.map(doc => (
        <div key={doc.id}>{doc.number}</div>
      ))}
    </div>
  )
}
```

## ⚠️ Возможные проблемы

### 1. CORS ошибка

**Проблема:** `Access to XMLHttpRequest has been blocked by CORS policy`

**Решение:** Убедитесь что backend запущен и разрешает запросы с `localhost:3000`

### 2. 401 ошибка

**Проблема:** Постоянные ошибки 401 Unauthorized

**Решение:** 
- Очистите localStorage
- Войдите заново через `/login`
- Проверьте что токены сохраняются

### 3. Network Error

**Проблема:** `Network Error` при всех запросах

**Решение:**
- Проверьте что backend запущен на `http://localhost:8000`
- Проверьте `NEXT_PUBLIC_API_URL` в `.env.local`
- Проверьте что нет ошибок в консоли backend

### 4. Данные не обновляются

**Проблема:** После изменения данных на backend, frontend не обновляется

**Решение:**
- Обновите страницу (F5)
- Проверьте что используется правильный токен
- Убедитесь что данные действительно изменились в БД

## 📚 Дополнительная документация

- [API_INTEGRATION.md](API_INTEGRATION.md) - Подробное руководство по API
- [backend/README.md](backend/README.md) - Документация Backend
- [README.md](README.md) - Общая документация проекта

## 🎯 Следующие шаги

После успешного подключения можно:

1. Подключить остальные страницы к API:
   - `/partners` - контрагенты
   - `/company` - профиль компании
   - `/documents/create` - создание документов

2. Добавить React Query для кеширования

3. Интегрировать OnlyOffice для редактирования

4. Добавить WebSocket для real-time обновлений

## ✅ Чек-лист готовности

- [ ] Docker Compose запущен
- [ ] Backend запущен на порту 8000
- [ ] Файл `.env.local` создан
- [ ] Frontend запущен на порту 3000
- [ ] Регистрация работает
- [ ] Логин работает
- [ ] Список документов загружается
- [ ] Swagger UI доступен

## 🆘 Нужна помощь?

Если что-то не работает:

1. Проверьте консоль браузера (F12)
2. Проверьте логи backend
3. Проверьте что Docker Compose работает: `docker-compose ps`
4. Проверьте переменные окружения
5. Попробуйте очистить localStorage и перезапустить

